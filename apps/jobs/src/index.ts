/**
 * `apps/jobs` — empty typed shell (workspace-foundation: Apps are empty
 * typed shells). No Trigger.dev task/config: the renewal-cron,
 * process-media, and sync-chatwoot jobs are deferred to later changes.
 */
export const APP_NAME = "@dirus/jobs" as const;
