/**
 * Drizzle schema barrel. Empty placeholder for Phase 2 (DB Driver & Tenant
 * Guards) — the §7.1/§7.2 tables land in Phase 3 (data-model schema).
 * `src/internal/client.ts` and `src/internal/admin.ts` already import this
 * module so `drizzle({ schema })` wiring is correct from day one.
 */
export {};
