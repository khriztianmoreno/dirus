/**
 * `apps/dashboard` — empty typed shell (workspace-foundation: Apps are empty
 * typed shells). No Vite app wiring: the exception-inbox UI is deferred to a
 * later change.
 */
export const APP_NAME = "@dirus/dashboard" as const;
